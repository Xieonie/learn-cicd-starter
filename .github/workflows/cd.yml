<<<<<<< HEAD
jobs:
  deploy:
    name: Deploy
=======
name: CD

on:
  push:
    branches: [main]

jobs:
  Deploy:
>>>>>>> bd215c1026a53bd7914fab06acf3b80623ac678c
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
<<<<<<< HEAD
          go-version: 1.22

      - name: Install goose
        run: go install github.com/pressly/goose/v3/cmd/goose@latest

      - name: Run migrations
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          goose -dir ./migrations sqlite "$DATABASE_URL" up

      # Ab hier kannst du dein normales Deployment starten
      - name: Build Docker image
        run: docker build -t my-app .

      - name: Deploy App
        run: echo "Hier dein Deploy-Schritt (z. B. Docker Push oder SSH Deploy)"
=======
          go-version: '1.21'   # oder die Version, die dein Projekt nutzt

      - name: Make build script executable
        run: chmod +x scripts/buildprod.sh

      - name: Run build script
        run: ./scripts/buildprod.sh
>>>>>>> bd215c1026a53bd7914fab06acf3b80623ac678c
